---
import BaseLayout from "@layouts/BaseLayout.astro";
import PageHero from "@components/PageHero.astro";
import CaseCard from "@components/CaseCard.astro";
import CtaBand from "@components/CtaBand.astro";
import { cases } from "@data/site";
import { withBase } from "@lib/urls";

const filters = ["Todos", "Conexão e regulação", "Projetos elétricos", "Perícia técnica", "Inteligência energética", "Eletromobilidade", "Geração distribuída"];
---

<BaseLayout
  title="Cenários de referência"
  description="Exemplos anonimizados de problemas técnicos que orientam a forma de atuação da Conexium Engenharia, sem identificação de clientes ou promessa de resultado."
  path="/cases/"
>
  <PageHero
    eyebrow="Cenários de referência"
    title="Problemas técnicos anonimizados, tratados com método e evidência."
    description="Exemplos anonimizados de problemas técnicos que orientam a forma de atuação da Conexium Engenharia, sem identificação de clientes, números inventados ou promessa de resultado."
    primaryHref={withBase("/contato/")}
    primaryLabel="Analisar um caso técnico"
  />

  <section class="bg-white px-5 py-24 sm:px-6 lg:px-8">
    <div class="mx-auto max-w-7xl">
      <div class="flex flex-wrap gap-2" aria-label="Filtros de cenários">
        {filters.map((filter) => (
          <button type="button" class="rounded-full border border-slate-200 px-4 py-2 text-sm font-semibold text-slate-700 transition hover:border-energy-400" data-case-filter={filter}>
            {filter}
          </button>
        ))}
      </div>
      <div class="mt-10 grid gap-5 lg:grid-cols-2" data-case-grid>
        {cases.map((study) => <CaseCard study={study} />)}
      </div>
    </div>
  </section>

  <CtaBand />

  <script>
    const buttons = document.querySelectorAll("[data-case-filter]");
    const cards = document.querySelectorAll("[data-case-card]");
    buttons.forEach((button) => {
      button.addEventListener("click", () => {
        const filter = button.getAttribute("data-case-filter");
        cards.forEach((card) => {
          const show = filter === "Todos" || card.getAttribute("data-case-card") === filter;
          card.classList.toggle("hidden", !show);
        });
      });
    });
  </script>
</BaseLayout>
